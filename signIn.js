function signInCheck(username_entered, password_entered){
  //dict["key"] --> key[list] (in list, 0 = pwd and 1 = email)

  //sign in test w/ info:
  userInfoList = [];
  userInfoList.push("b", "c@c.com");
  username = "a"
  userDict["username"] = userInfoList

  if (password_entered == userDict["username_entered"][0]) {
    // need to send user to home screen
    console.log(userDict["username_entered"][0])
    alert(correct)
  }
  // else {
  //   alert(Your username/password was not correct.);
  // };
};
